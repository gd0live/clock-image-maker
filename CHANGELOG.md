# Changelog

## 2026-06-05

### Added

- 시계 안쪽 미디어 입력에서 영상 파일을 선택하고 프리뷰에서 재생/일시정지할 수 있도록 추가했습니다.
- 영상 프레임 위에 기존 시계와 시계 밖 배경을 합성해 WEBM으로 저장하는 기능을 추가했습니다.
- 영상 내보내기는 시작 위치를 지정하고 최대 10초 길이로 제한해 저장할 수 있습니다.
- 시계 밖 영역을 별도 배경 그림으로 채우는 기능과 확대, X/Y 위치, 투명도 조절을 추가했습니다.
- 시계 밖 배경을 `그림` 또는 `색상` 중 하나로 선택하도록 정리하고, 색상 모드에서는 투명도와 색상만 조절되도록 변경했습니다.
- 시계 색상과 테두리 색상, 테두리 두께를 별도로 조절할 수 있도록 추가했습니다.
- 시계 테마에 `Edge Lines`, `Quarter Minimal`, `Dash Ring`, `Open Face`를 추가했습니다.
- 시계 테마에 `Custom`을 추가하고, Custom 선택 시에만 테두리, 눈금, 시/분침, 중심점 세부 메뉴가 표시되도록 변경했습니다.
- `월일표시`를 별도 버튼에서 Custom의 `시/분침 스타일` 옵션으로 이동했습니다.
- Custom 시계 메뉴를 시계, 눈금, 테두리 행으로 재배치하고 눈금 색상을 별도로 조절할 수 있도록 추가했습니다.
- 현재 `index.html`은 `index.backup-video-overlay.html`로 백업했습니다.

## 2026-06-04

Clock Image Maker의 편집 흐름, 저장 방식, 이미지 입력, 내보내기 기능을 전반적으로 정리한 업데이트입니다.

### Added

- 클립보드 붙여넣기(`Ctrl+V`)와 프리뷰 영역 드래그 앤 드롭으로 배경 이미지를 바로 불러올 수 있습니다.
- 시계 바늘 대신 현재 선택한 시간 값을 `n월`, `n일` 표식으로 표시하는 `월일표시` 옵션을 추가했습니다.
- PNG뿐 아니라 WEBM 동영상 내보내기를 지원합니다. GIF 배경을 사용할 때 움직임을 유지한 채 시계 오버레이를 저장할 수 있습니다.
- 저장 버튼에 내보내기 형식 선택 메뉴를 추가해 PNG와 WEBM을 선택할 수 있습니다.
- 프리뷰 영역 상단에 에디터 메뉴 바를 추가했습니다. 새 작업, 브라우저 저장, 내보내기, 가져오기, 실행 취소, 다시 실행을 한곳에서 사용할 수 있습니다.
- VSCode 스타일의 작업 탭 UI를 추가했습니다. 열린 작업만 탭으로 표시되고, 활성 탭 이름을 바로 수정할 수 있습니다.
- 설정 변경 이력을 위한 실행 취소/다시 실행 기능을 추가했습니다.
- `.codex/project-memory.md`에 프로젝트 작업 선호와 주의사항을 기록했습니다.

### Changed

- 기존 저장 슬롯 중심 UI를 열린 탭 중심의 작업 공간으로 개편했습니다.
- 열린 탭 목록, 활성 탭, 사용자 지정 탭 이름을 IndexedDB에 저장하도록 변경했습니다.
- `Ctrl+S`는 현재 작업을 브라우저 저장소에 저장하고, 이미지 내보내기는 `Ctrl+Shift+S`로 실행되도록 단축키를 정리했습니다.
- 브라우저 저장 명령은 덮어쓰기 확인 없이 실행되며, 저장할 변경 사항이 없을 때는 비활성화됩니다.
- 설정 패널에 중복으로 있던 새 작업, 저장, 내보내기, 가져오기, 실행 취소, 다시 실행 버튼을 제거했습니다.
- 시간, 분, `월일표시` 컨트롤이 한 줄에서 함께 동작하도록 시간 설정 영역을 정리했습니다.
- `시간표시` 토글을 텍스트 입력 필드 옆으로 옮겨 문구 편집 흐름을 단순화했습니다.
- 파일 선택, 클립보드 붙여넣기, 드래그 앤 드롭이 같은 이미지 로딩 경로를 사용하도록 통합했습니다.
- 배경 이미지를 자르지 않고 전체가 보이도록 `cover` 방식에서 `contain` 방식으로 렌더링을 변경했습니다.
- 모바일에서는 떠 있는 설정 버튼 대신 왼쪽 레일과 슬라이드 패널로 설정 접근 방식을 변경했습니다.
- 모바일 메뉴 바가 화면 밖으로 넘치지 않도록 여러 줄로 감기게 조정했습니다.
- 이미지 입력 안내 문구에 파일 선택, 붙여넣기, 드래그 앤 드롭을 모두 반영했습니다.
- GIF 내보내기 메뉴는 제거했습니다. 브라우저에서 큰 GIF를 안정적으로 재인코딩하기 어려워 WEBM 저장으로 대체했습니다.
- 이전 설정에 남아 있을 수 있는 `gif` 내보내기 형식은 PNG로 자동 보정되도록 했습니다.

### Fixed

- 붙여넣거나 드롭한 이미지가 프리뷰와 내보내기 결과에서 잘려 보이던 문제를 수정했습니다.
- 프리뷰 확대/축소가 실제 내보내기 좌표와 렌더링 결과에 영향을 주던 문제를 수정했습니다.
- 모바일에서 캔버스 CSS 때문에 확대/축소가 무시되던 문제를 수정했습니다.
- 시계 링 두께가 고정 픽셀로 계산되어 시계 크기 변경 시 어색하게 보이던 문제를 수정했습니다.
- `월일표시` 표식의 프리뷰와 PNG 내보내기 위치 및 글자 크기가 서로 다르던 문제를 수정했습니다.
- 눈금이 없는 테마에서도 월/일 표식이 서로 구분되도록 위치 계산을 조정했습니다.
- 탭 이름 수정 후 작업 탭 전환이 어긋나던 문제를 수정했습니다.
- 탭 닫기 버튼의 정렬을 SVG 아이콘 기반으로 개선했습니다.
- PNG 또는 WEBM 생성 중 저장 버튼을 반복 클릭해 중복 내보내기가 실행되는 문제를 방지했습니다.
- 큰 파일 저장 시 다운로드 URL이 너무 빨리 해제되어 파일이 잘릴 수 있던 문제를 완화했습니다.

### Notes

- 현재 프로젝트는 `index.html` 중심의 단일 파일 웹 앱입니다.
- 브라우저별 WEBM/GIF 처리 능력 차이가 있어 GIF 배경의 WEBM 저장은 Chrome 또는 Edge 환경을 기준으로 지원합니다.
